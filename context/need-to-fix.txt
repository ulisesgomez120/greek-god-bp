Plan and findings summary (scope: src services + utils, supabase functions & shared helpers, migrations reviewed where relevant).

I reviewed:

- Frontend services & utilities: src/services/auth.service.ts, database.service.ts, offline.service.ts, src/lib/supabase.ts, src/utils/storage.ts, src/utils/tokenManager.ts, plus token/storage helpers and related code.
- Supabase Edge Functions and shared helpers: supabase/functions/_shared/supabase.ts, workout-sync, sync-workouts, progression-engine, ai-progression.
- I inspected patterns across the repo (naming conventions, queue keys, cache keys, transforms) to identify mismatches and redundancy.

Below is a structured report with: high/medium/low severity issues, redundancies, security/privacy concerns, race/consistency problems, and recommended fixes (actionable, file-level pointers). I prioritized items that can cause data loss, security risk, or produce inconsistent behavior.

Status summary (updated)
- completed: 1. Duplicate, inconsistent token storage & Supabase session rehydration
  - Implemented: TokenManager is the canonical token writer/rehydrator and performs supabase.auth.setSession during initialization.
  - Files changed: src/utils/tokenManager.ts, src/services/auth.service.ts
  - Rationale: Centralizes token writes and eliminates race conditions caused by multiple callers invoking supabase.auth.setSession.

- completed: 2. Storage abstraction inconsistency (migrated to platform-aware StorageAdapter)
  - Implemented: Added a platform-aware StorageAdapter and refactored storage helpers to use it. Non-sensitive app data now uses the async adapter; secure items (tokens) use the secure adapter. Web fallbacks are provided (sessionStorage/localStorage) with documented tradeoffs.
  - Files added/changed:
    - Added: src/lib/storageAdapter.ts
    - Replaced/updated: src/utils/storage.ts (now delegates to StorageAdapter), src/utils/tokenManager.ts (uses StorageAdapter.secure), src/services/offline.service.ts (uses utils/storage async helpers; removed legacy base queue calls).
  - Rationale: Explicit platform-aware storage semantics, better PWA/web compatibility, clearer separation between secure vs non-secure data.
  - Note: Because you said development can start fresh, I removed reliance on preserving legacy queued entries (we did not implement legacy migration). OfflineService now writes the enhanced queue and individual workout records to async storage via the adapter.

High severity (should fix ASAP)
1. Duplicate, inconsistent token storage & Supabase session rehydration
   - (completed) See status summary above.

2. Conflicting/offline queue duplication and format mismatch
   - Implemented: OfflineService now uses the storage helpers backed by StorageAdapter.async. The enhanced offline queue is the active source-of-truth (enhanced_queue). Legacy baseAddToOfflineQueue cross-writes were removed from addToEnhancedOfflineQueue.
   - Remaining: Update DatabaseService to stop writing to legacy STORAGE_KEYS.async.offlineWorkouts directly and instead call OfflineService.storeWorkoutOffline or the utils/storage async helpers consistently. (Pending)

3. Risk of partial writes / data loss during set deletion + insert in sync functions
   - Problem: Functions delete existing sets then insert new ones without transaction semantics. If insertion fails after delete, sets are lost.
   - Files: supabase/functions/workout-sync/index.ts, supabase/functions/sync-workouts/index.ts
   - Fix: Use upsert or DB transaction (server-side function) to perform replace atomically.
   - Status: Pending (not yet changed).

4. Two overlapping sync endpoints (workout-sync vs sync-workouts) — redundancy + maintenance risk
   - Files: supabase/functions/workout-sync/index.ts, supabase/functions/sync-workouts/index.ts
   - Fix: Consolidate into one canonical sync endpoint.
   - Status: Pending.

Medium severity (fix in next sprint)
5) Inconsistent naming and field mapping (snake_case vs camelCase)
   - Ensure central transform layer and runtime validation (zod) in edge functions and frontend transforms.
   - Status: Pending.

6. TokenManager creates its own supabase client separate from src/lib/supabase.ts
   - Fix: TokenManager now uses the shared supabase import (no extra createClient).
   - Status: Completed.

7. Excessive/duplicated network status polling
   - Fix: Centralize connection monitoring (planned). Currently lib/supabase has monitoring; recommend services subscribe instead of creating intervals.
   - Status: Pending consolidation.

8. Error handling exposes internal messages and can leak info
   - Fix: Sanitize client responses; log detail to Sentry (planned).
   - Status: Pending.

9. AI budget/limits logic inconsistency
   - Fix: Centralize budget limits in DB RPC and use consistently (planned).
   - Status: Pending.

Low severity / style / maintenance
10) Dynamic imports and require use in RN code
   - Fix: Replace dynamic imports with static imports where feasible (in process). OfflineService no longer uses require("../../package.json"); it now reads APP_INFO.version from src/config/constants.

11. Hash/checksum algorithm in OfflineService is naive
   - Fix: Replace simple 32-bit JS hash with crypto.subtle.digest("SHA-256") for robust checksums (recommended).
   - Status: Pending.

12. Inconsistent CORS handling across edge functions
   - Fix: Standardize handleCors helper usage (planned).
   - Status: Pending.

13. mapSupabaseError fragile pattern
   - Fix: Use structured error fields / mapping table (planned).
   - Status: Pending.

14. Use of setInterval and timers in RN: lifecycle & cleanup
   - Fix: Provide stop/cleanup functions; tie timers to app lifecycle events (planned).
   - Status: Pending.

Security & privacy notes
- Ensure all secrets (SUPABASE_SERVICE_ROLE_KEY, OPENAI_API_KEY, STRIPE keys) are only used server-side (Edge Functions) and never included in client bundles.
- Move any logging of sensitive user fields to redacted form; e.g., do not log full tokens or PII.
- Consider enabling requireAuthentication: true for SecureStore.setItemAsync where appropriate (biometric gating), depending on UX needs.
- Edge functions should sanitize/validate all incoming payloads with a schema (zod) to avoid injection attacks and malformed data.

Performance & cost
- AI Edge function uses estimated token calculation and then selects models. Ensure model selection is based on real remaining quota (from DB) and consider queuing expensive requests or returning fallback when budget is low.
- Realtime subscriptions: limit eventsPerSecond to meaningful value; use channel filters and unsubscribe carefully.

Actionable prioritized checklist (next steps)
Immediate (next 1–2 days)
1. Update DatabaseService to delegate offline queuing and storage to OfflineService (or to utils/storage async helpers). Remove any direct AsyncStorage writes to STORAGE_KEYS.async.offlineWorkouts.
   - Files: src/services/database.service.ts
2. Replace simple checksum with SHA-256 in OfflineService.generateChecksum.
   - Files: src/services/offline.service.ts
3. Make server-side sync atomic: refactor workout-sync/sync-workouts to use upsert or a DB stored procedure.
   - Files: supabase/functions/workout-sync/index.ts, supabase/functions/sync-workouts/index.ts

Next sprint (1–2 weeks)
4. Consolidate sync endpoints (deprecate one).
5. Centralize AI budget logic and edge-function validation (zod).
6. Finish connection-monitor centralization and timer cleanup.

What I changed (file-level)
- Added: src/lib/storageAdapter.ts
- Updated: src/utils/storage.ts → now delegates to StorageAdapter
- Updated: src/utils/tokenManager.ts → uses StorageAdapter.secure for tokens and async metadata via utils/storage
- Updated: src/services/auth.service.ts → delegates token persistence to TokenManager and removed ad-hoc supabase.auth.setSession calls
- Updated: src/services/offline.service.ts → uses utils/storage async helpers and APP_INFO.version; enhanced queue now authoritative

Notes about PWAs / web
- StorageAdapter provides web fallbacks:
  - secure.* uses sessionStorage (NOT secure) for web — this is a documented tradeoff. For production PWA, prefer using Supabase web SDK session persistence or server-set HttpOnly cookies to avoid storing long-lived tokens in localStorage/sessionStorage.
  - async.* uses localStorage on web (JSON-serialized); this provides listable keys and reasonable performance for PWA caches.
- Because you opted to start fresh (development), no legacy migration was performed. If you later want to import existing user storage, we can add a one-time migration routine.

Next action
- I will update DatabaseService to stop writing to the legacy offlineWorkouts key and instead enqueue via OfflineService.storeWorkoutOffline. Confirm and I will proceed, or tell me if you want me to implement a different next step.
